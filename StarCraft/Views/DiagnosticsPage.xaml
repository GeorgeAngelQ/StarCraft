<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StarCraft.Views.DiagnosticsPage"
             Title="Diagnóstico"
             BackgroundColor="#0F0F0F">

    <ScrollView>
        <Grid Padding="20" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- ENCABEZADO -->
            <Frame Grid.Row="0"
                   BackgroundColor="#1A1A1D"
                   BorderColor="#2D2D30"
                   CornerRadius="12"
                   HasShadow="True"
                   Padding="20">
                <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                    <Label Text="🔧" FontSize="32" VerticalOptions="Center" />
                    <Label Text="Diagnóstico del Sistema" 
                           FontSize="24" 
                           FontAttributes="Bold"
                           TextColor="#6C63FF"
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Frame>

            <!-- INFORMACIÓN -->
            <Frame Grid.Row="1"
                   BackgroundColor="#1A1A1D"
                   BorderColor="#2D2D30"
                   CornerRadius="12"
                   HasShadow="True"
                   Padding="20">
                <ScrollView>
                    <Label x:Name="LblDiagnostico"
                           Text="Cargando información..."
                           TextColor="#E0E0E0"
                           FontFamily="Courier New"
                           FontSize="12"
                           LineBreakMode="WordWrap"/>
                </ScrollView>
            </Frame>

            <!-- BOTONES DE ACCIÓN -->
            <Frame Grid.Row="2"
                   BackgroundColor="#1A1A1D"
                   BorderColor="#2D2D30"
                   CornerRadius="12"
                   HasShadow="True"
                   Padding="20">
                <Grid ColumnSpacing="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Text="📋 Copiar Info"
                            BackgroundColor="#6C63FF"
                            TextColor="White"
                            FontAttributes="Bold"
                            FontSize="14"
                            CornerRadius="8"
                            HeightRequest="50"
                            Clicked="OnCopiarInfoClicked">
                        <Button.Shadow>
                            <Shadow Brush="#6C63FF" Opacity="0.5" Radius="15" Offset="0,5" />
                        </Button.Shadow>
                    </Button>

                    <Button Grid.Column="1"
                            Text="🔄 Recrear DB"
                            BackgroundColor="#FF6B6B"
                            TextColor="White"
                            FontAttributes="Bold"
                            FontSize="14"
                            CornerRadius="8"
                            HeightRequest="50"
                            Clicked="OnRecrearDBClicked">
                        <Button.Shadow>
                            <Shadow Brush="#FF6B6B" Opacity="0.5" Radius="15" Offset="0,5" />
                        </Button.Shadow>
                    </Button>
                </Grid>
            </Frame>
        </Grid>
    </ScrollView>
</ContentPage>